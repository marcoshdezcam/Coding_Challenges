#include <iostream>
using namespace std;

long repeatedString(string s, long n) {
    // s.size() mod n = finalPos;
    // As_untilFinalPos = s.find(')
    // n / s.size() = numberOfRepetitions
    // numberOf_A = s.find('a')
    // total_As = (numberOf_A * numberOfRepetitions) + (As_untilP)
}

int main(){
    return 0;
}
